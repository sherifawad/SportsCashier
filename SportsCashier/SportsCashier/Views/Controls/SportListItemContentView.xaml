<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SportsCashier.Views.Controls.SportListItemContentView">
        <StackLayout>
            <BoxView BackgroundColor="LightGray"  HeightRequest="2"/>
            <Grid ColumnDefinitions="*, *" Margin="0,5,0,0">

                <Label Text="{Binding SportCaegory.SportType}" 
                                           FontSize="Title" TextColor="Black"
                                           LineBreakMode="WordWrap"   HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                <Label Grid.Column="1" Text="{Binding SportName}"
                                           FontSize="Title" TextColor="Black"
                                           LineBreakMode="WordWrap" 
                                               HorizontalTextAlignment="Center" VerticalTextAlignment="Center"/>


            </Grid>
            <BoxView BackgroundColor="LightGray"  HeightRequest="2"/>
        </StackLayout>
</ContentView>